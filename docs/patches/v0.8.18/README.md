# SSTcore v0.8.18 P0 canon/build fix

Physics-only subset of `sstcore_v0.8.18_p0_build_canon_fix_git.diff` — **flat include-tree hunks omitted**; repo keeps `../include/` layout.

Apply from repo root (after manual integration or):

```powershell
git apply -p0 docs/patches/v0.8.18/0001-build-canon-fix.patch
```

See [audits/v0.8.18/README.md](../../audits/v0.8.18/README.md) for P0/P1 traceability.
